---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  actionHref?: string;
  actionLabel?: string;
}

const { eyebrow, title, description, actionHref, actionLabel } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
---

<div class="mb-8 flex flex-col gap-4 md:mb-10 md:flex-row md:items-end md:justify-between">
  <div class="space-y-3">
    {eyebrow && <p class="eyebrow">{eyebrow}</p>}
    <h2 class="font-display text-3xl font-semibold tracking-tight text-[var(--text-main)] md:text-4xl">{title}</h2>
    {description && <p class="max-w-2xl text-base leading-8 text-[var(--text-muted)]">{description}</p>}
  </div>
  {actionHref && actionLabel && (
    <a href={`${baseUrl}${actionHref.replace(/^\//, "")}`} class="button-secondary shrink-0">{actionLabel}</a>
  )}
</div>